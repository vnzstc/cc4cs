File "core.py": includes all the main operations of the framework 

Global Variables: 
1 - scriptPath: path of the launch.sh
2 - frameworkPath: the path in which the framework resides 
3 - prjPath: the path in which the test function resides 

Function Resume: 

## OS operation

### Dir operations 
- checkDir(dirPath):
- removeDir(dirName):
- createDir(dirName):

### File operations

#### File creation 
- createFile(filename, extension="")
- closeFile(fileObject):
- createFileWriter(fileDescriptor):

#### File writing 
- writeTuple(label, value, writerObject):

#### File moving 
- moveFile(destination, extension):
- moveAllFiles(destination):

#### File general 
- findFileByExtension(directory, extension):
- returnFiles(topDir, dirFlag = False, extension = 
None)
- splitFilename(filename):
- parseGcovOutput():
- parseSimulationOutput(simFilename):


### Command Management

- executeCommandSet(resultFile, microName, headers):
- getOutputFilename(commandString):


### Other functions 

- splitBySpace(customString):
- printMicroprocessors():
- chooseMicro():


### Frama-C 

- expandCommand(reducedCommand, directory = None):
- executeFramaC(directory):

- readFramaCOutput(directory, filePath, regex, splitToken, linesNumber = 0):

- createFramaCSV(outputFilename, inputDict):
- staticAnalysis(filename, directory):
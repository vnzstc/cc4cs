File "inputgenerator.py": includes all the main operations of the framework 

Global Variables: 

types: 
scalars: 
arrays:
sizes: 

Function Resume: 

pascalCase(inputStr):

replaceStr(filename, regexStr, replacementStr):

getListfromRegex(regexStr, lineStr):

searchRegex(regexStr, content):

initializeSizes(variable):

getSizes(variable):

parametersFilter(lineStr, targeType, indexType):

checkInput(regexStr, inputStr):	

askForInput():

updateDict(dictionary, key, regex, inputString):

getParametersFromFile(jsonFileObject, currentType):

discoverParameters(filename, targeType, indexType):

splitScalarInput(rangeStr):

genRandomList(rangeMin, rangeMax, elementNum, varType):

generateList(lst):

listCreator():

createHeader():

closeHeader(fileObject):

removeLastOccurence(character, string):

writeArray(headerFile, value, varType, arraySizes):

writeVariables(combination, headerFile):
generateHeaders():